Create alt image descriptions for all of the uploaded exam diagram images in key value pairs of the image file name as key and description as value. Output as JSON array of object with properties "alt" (string), and "isValid" (boolean). If the image is a not a valid diagram or illustration (e.g., a written answer field), "isValid" must be false. Output just the json, no code tags and nothing else.
